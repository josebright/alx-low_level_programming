#include "main.h"

/**
 * _islower - prints 1 or 0 depending on input
 * @alph: first parameter
 *
 * Description: prints all lowercase letters
 * Return: Always(0).
 */

int _islower(int alph)
{
	return (alph >= 'a' && alph <= 'z');
}
